# 提交改动

小改动先写清楚要解决什么，再给测试。不要为了加一个协议，把系统代理控制、账号管理或订阅数据库一起搬进来。

几条不太能商量的边界：

- 测试只能用合成凭据和模拟上游，不把个人环境做成 fixture。
- 业务日志不能出现请求正文、认证、完整 state、订阅 URL、节点名称或代理密码。
- state 必须绑定凭据和出口；响应观测不能直接覆盖 active。
- 不自动重放生成请求，不在 401/403/429 后继续换节点尝试。
- 任何 Codex 配置修改都要有独立备份、恢复记录和冲突保护。
- 新协议或新平台要分清“能解析”“能编译”和“实际跑通”，不要混写成已支持。

提交前运行 `go vet ./...`、`go test -race ./...`，检查 Windows 路径、换行和文件锁。涉及文案时，给用户写下一步能做什么，少写宏大的效果承诺。

没有对应测试的“清理代码”容易把稳定性清理掉。改状态机时请同时覆盖过期、并发、旧响应和取消；改配置写入时覆盖崩溃与用户中途编辑。
